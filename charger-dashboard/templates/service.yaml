apiVersion: v1
kind: Service
metadata:
  name: {{ include "charger-dashboard.fullname" . }}
  labels:
    {{- include "charger-dashboard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "charger-dashboard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
